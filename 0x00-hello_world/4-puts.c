#include <stdio.h>
/**
 * main - the code block prints out the characters in the parantheses
 *
 * Return: the return value should be zero
 */
int main(void)
{
	putc("\ "Programming is like building of a multiligual puzzle");
	return (0);
}
